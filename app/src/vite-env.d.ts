
/** Injected by vite.config.ts `define` — the deploy's commit, host and deploy context. */
declare const __BUILD_REF__: string;
declare const __BUILD_CONTEXT__: string;
/** Whether the Vercel runtime is there: "vercel" or "local". See the note in vite.config.ts. */
declare const __BUILD_HOST__: string;
